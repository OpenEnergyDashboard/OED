const migrationList = require('./registerMigration');

class Graph {
	constructor(v) {
		this.v = v;
	}


}


const arrayLength = migrationList.length;

const adjListArray = [];

for (let i = 0; i < arrayLength; i++) {
	ad
}

const graph = {
	0.1: 0.2,
};

for (let i = 0; i < arrayLength; i++) {
	console.log(migrationList[i].fromVersion);
	console.log(migrationList[i].toVersion);
}
